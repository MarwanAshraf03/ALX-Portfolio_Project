backend/
├── express/
│   ├── server/
│   │   ├── controllers/      # Contains the logic for handling requests
│   │   ├── models/           # Contains the Mongoose models (for MongoDB)
│   │   ├── routes/           # Contains the route definitions
│   │   ├── server.js         # Main entry point to the Express server
│   │   ├── config.js         # Configuration files, such as DB or Redis config
│   │   └── utils/            # Helper functions or utilities
│   ├── node_modules/         # Node.js dependencies (managed by Docker volumes)
│   ├── package.json          # Contains project dependencies and scripts
│   ├── package-lock.json     # Locks down dependency versions
│   └── Dockerfile            # Dockerfile for Express application
├── mongo/                    # Data for MongoDB (stored in Docker volume)
├── mosquitto/                # MQTT configuration files
│   └── config/
│       └── mosquitto.conf    # Configuration for Mosquitto
├── redis/                    # Redis data (stored in Docker volume)
└── python/                   # Python related scripts
    ├── requirements.txt      # Python dependencies
    ├── main.py               # Main entry point for the Python application
    └── Dockerfile            # Dockerfile for Python application

project-root/
├── docker-compose.yml        # Docker Compose file placed at the root
├── backend/
│   ├── express/
│   ├── mongo/
│   ├── redis/
│   ├── mosquitto/
│   └── python/
└── frontend/
    ├── public/
    ├── src/
    └── Dockerfile
